apiVersion: v1
kind: Service
metadata:
  name: plenartrend-crud
  namespace: plenartrend
spec:
  selector:
    app: plenartrend-crud
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  type: ClusterIP