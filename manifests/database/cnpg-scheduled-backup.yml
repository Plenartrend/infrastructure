apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: postgresql-db-scheduled-backup
  namespace: plenartrend
spec:
  cluster:
    name: postgresql-db
  schedule: "0 0 0 * * *"
  method: barmanObjectStore
  immediate: true
  backupOwnerReference: self

